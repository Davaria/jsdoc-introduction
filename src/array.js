/**
 * Lista de edades de usuarios
 * @type {Array}
 */
const age = [19, 20, 21, 22];

/**
 * Array of User's Scores
 * @type {Array<Number>}
 */
const scores = [10, 199.99, 299.99, 30300.9];

/**
 * Array of numbers and strings
 * @type {Array<string | number>}
 */
const scoresAndString = [1, 2, "3"];
