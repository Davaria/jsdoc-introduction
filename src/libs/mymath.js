/**
 * My Math library
 * @module MyMath
 */

/**
 * Add two numbers
 * @param {number} x first number
 * @param {number} y second number
 * @returns {number} Sum of x and y
 */
const addNumber = (x, y) => x + y;

/**
 * Subtract two number
 * @param {number} x first number
 * @param {number} y second number
 * @returns {number} Substract of x and y
 */
const substract = (x, y) => x - y;

module.exports = { addNumber, substract };
