/**
 * Add two numbers
 * @param {number } a1 fisrt number
 * @param {number } a2 second number
 * @returns {number} total result
 */
function add(a1, a2) {
  return a1 + a2;
}

/**
 * Add two number and returns a string
 * @param {number} n1 first number
 * @param {number} n2 second number
 * @returns {string}
 */
const addTwo = (n1, n2) => `The result is ${n1 + n2}`;

console.log(add(1, 2));
